apiVersion: apps/v1
kind: Deployment
metadata:
  name: ignored-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ignored
  template:
    metadata:
      labels:
        app: ignored
    spec:
      containers:
        - name: app
          image: nginx:latest
