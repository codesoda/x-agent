DECISION: REWORK
ITERATION: 35
SPEC: 10-sql-agent.md

SUMMARY:
The sql-agent implementation follows the established runner pattern and is integrated across script, SKILL, install, and README. Core functionality is implemented, but CI safety around formatting mode is incorrect: `RUN_FIX` can still trigger mutation in CI.

BLOCKERS:
None

MAJOR ISSUES:
- [R-001] [skills/sql-agent/scripts/sql-agent.sh:231-233] `all` enables `fix` via `RUN_FIX` before evaluating CI semantics, so `CI=true` + `RUN_FIX=1` still runs `sqlfluff fix`. `resolve_fmt_mode()` only coerces `FMT_MODE` to `check`, but `fix_enabled` still uses `RUN_FIX || FMT_MODE == fix`. This breaks FR-10 intent (`CI` should force check-only) and can mutate files in CI runs, invalidating `run-scope`/report assumptions.

MINOR ISSUES:
- [R-002] [tests/sql-agent/clean-dialect] Missing from the fixture set; dialect-specific pass-through behavior (`SQLFLUFF_DIALECT`) is only documented, not asserted by scenarios. This weakens regression coverage for FR-2/FR-10 behaviors tied to tool behavior by dialect.
- [R-003] [skills/sql-agent/scripts/sql-agent.sh] The script is 259 lines; the spec target for thin domain-specific agent logic is "~200 lines." This is close, but above the stated guideline and can creep over time unless kept explicit.

NEXT STEPS:
Return to WORK phase with same plan to fix issues

---
FEEDBACK FOR NEXT PHASE:

Priority fixes:
1. [R-001] In `skills/sql-agent/scripts/sql-agent.sh`, compute `fix_enabled` after CI-aware mode resolution and gate fix execution on `FMT_MODE == fix` only (or add explicit `CI` check) so `RUN_FIX=1` cannot cause `sqlfluff fix` in CI.
2. Add a regression assertion in `tests/sql-agent/ci-check` that checks for no mutation or fix-step execution under `CI=true` while `RUN_FIX=1` / `FMT_MODE=fix`, so check-only behavior is enforced by test output/fixtures.

Edge cases to handle:
1. [R-001] `RUN_FIX=1` in CI should never run mutating steps, including explicit `all` and any command path that implies formatting.

Test gaps:
1. [R-002] Add `tests/sql-agent/clean-dialect/` with `SCENARIO_NAME="sql-agent clean-dialect"`, `SQLFLUFF_DIALECT=mysql`, and a SQL syntax-valid fixture for that dialect to assert dialect pass-through end-to-end.
2. [R-001] Extend CI-mode fixture(s) to assert `fix` is skipped or treated as check-only in CI when `FMT_MODE=fix` and/or `RUN_FIX=1`.
