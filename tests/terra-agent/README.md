# terra-agent Scenarios

Placeholder fixture folders for the upcoming `terra-agent`.

Add runnable scenarios here once `skills/terra-agent/scripts/terra-agent.sh` exists:

- `clean/` with a passing Terraform fixture and `scenario.env`
- `issues/` with a failing Terraform fixture and `scenario.env`

